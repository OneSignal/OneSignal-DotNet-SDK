<?xml version="1.0" encoding="utf-8" ?>
<ContentView
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  x:Class="OneSignalDemo.Controls.Sections.UserSection"
  x:Name="root"
>
  <VerticalStackLayout Spacing="8">
    <!-- Section header -->
    <Label Text="USER" Style="{StaticResource SectionHeaderStyle}" Padding="4,0,0,0" />

    <!-- User status card -->
    <Border Style="{StaticResource CardBorderStyle}">
      <VerticalStackLayout>
        <Grid Padding="12" ColumnDefinitions="*,Auto">
          <Label
            Grid.Column="0"
            Text="Status"
            Style="{StaticResource BodyMediumStyle}"
            VerticalOptions="Center"
          />
          <Label
            Grid.Column="1"
            x:Name="StatusLabel"
            Text="Anonymous"
            Style="{StaticResource BodySmallStyle}"
            FontFamily="DroidSansMono"
            VerticalOptions="Center"
            AutomationId="user_status_label"
          />
        </Grid>
        <BoxView HeightRequest="1" Color="{StaticResource OsDivider}" Margin="12,0" />
        <Grid Padding="12" ColumnDefinitions="*,Auto">
          <Label
            Grid.Column="0"
            Text="External ID"
            Style="{StaticResource BodyMediumStyle}"
            VerticalOptions="Center"
          />
          <Label
            Grid.Column="1"
            x:Name="ExternalIdLabel"
            Text="–"
            Style="{StaticResource BodySmallStyle}"
            FontFamily="DroidSansMono"
            VerticalOptions="Center"
            AutomationId="external_id_label"
          />
        </Grid>
      </VerticalStackLayout>
    </Border>

    <!-- Login/Logout buttons -->
    <Button
      x:Name="LoginButton"
      Text="LOGIN USER"
      Style="{StaticResource PrimaryButtonStyle}"
      Clicked="OnLoginClicked"
      AutomationId="login_button"
    />
    <Button
      x:Name="LogoutButton"
      Text="LOGOUT USER"
      Style="{StaticResource OutlineButtonStyle}"
      IsVisible="False"
      Clicked="OnLogoutClicked"
      AutomationId="logout_button"
    />
  </VerticalStackLayout>
</ContentView>
